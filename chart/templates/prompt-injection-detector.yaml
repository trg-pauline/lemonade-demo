---
apiVersion: serving.kserve.io/v1alpha1
kind: ServingRuntime
metadata:
  annotations:
    helm.sh/weight: "0"
    opendatahub.io/recommended-accelerators: '["nvidia.com/gpu"]'
    openshift.io/display-name: Prompt Injection Detector Runtime
  name: prompt-injection-detector-runtime
  labels:
    opendatahub.io/dashboard: 'true'
spec:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: '8080'
  containers:
    - args:
        - '--workers'
        - '4'
        - '--host'
        - 0.0.0.0
        - '--port'
        - '8000'
        - '--log-config'
        - /common/log_conf.yaml
      command:
        - uvicorn
        - 'app:app'
      env:
        - name: MODEL_DIR
          value: /mnt/models
        - name: HF_HOME
          value: /tmp/hf_home
      image: quay.io/trustyai/guardrails-detector-huggingface-runtime:latest
      name: kserve-container
      ports:
        - containerPort: 8000
          protocol: TCP
  multiModel: false
  supportedModelFormats:
    - autoSelect: true
      name: guardrails-detector-huggingface
---
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  annotations:
    helm.sh/weight: "1"
    openshift.io/display-name: prompt-injection-detector
    serving.kserve.io/deploymentMode: RawDeployment
  name: prompt-injection-detector
  labels:
    opendatahub.io/dashboard: 'true'
spec:
  predictor:
    automountServiceAccountToken: false
    maxReplicas: 1
    minReplicas: 1
    model:
      modelFormat:
        name: guardrails-detector-huggingface
      name: ''
      resources:
        limits:
          cpu: {{ .Values.detectors.promptInjection.resources.limits.cpu }}
          memory: {{ .Values.detectors.promptInjection.resources.limits.memory }}
          {{- if .Values.detectors.promptInjection.useGpu }}
          nvidia.com/gpu: '1'
          {{- end }}
        requests:
          cpu: {{ .Values.detectors.promptInjection.resources.requests.cpu }}
          memory: {{ .Values.detectors.promptInjection.resources.requests.memory }}
          {{- if .Values.detectors.promptInjection.useGpu }}
          nvidia.com/gpu: '1'
          {{- end }}
      runtime: prompt-injection-detector-runtime
      storage:
        key: minio-data-connection-detector-models
        path: deberta-v3-base-prompt-injection-v2
    {{- if .Values.detectors.promptInjection.useGpu }}
    tolerations:
      - effect: NoSchedule
        key: nvidia.com/gpu
        operator: Exists
    {{- end }}
